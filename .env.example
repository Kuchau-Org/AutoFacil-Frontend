# Variables de entorno del frontend de AutoFacil. Copie este archivo como `.env`.

# URL base del backend (FastAPI). Si se omite, por defecto es http://localhost:8000
VITE_API_URL=http://localhost:8000
